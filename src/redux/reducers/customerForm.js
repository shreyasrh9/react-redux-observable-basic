import * as actionTypes from '../../actions/actionTypes'

const initialState = {
    loadSpinner: true,
};


export const cusotmerForm = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
